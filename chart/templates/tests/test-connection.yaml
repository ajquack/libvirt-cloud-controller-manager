apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "libvirt-cloud-controller-manager.fullname" . }}-test-connection"
  labels:
    {{- include "libvirt-cloud-controller-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "libvirt-cloud-controller-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
